# test that chezmoi apply normalizes symlinks
chezmoi apply $HOME${/}.symlink_forward_slash
cmp $HOME/.symlink_forward_slash $HOME/.dir/file

# test that the persistent state matches the actual state
chezmoi diff
! stdout .
chezmoi status
! stdout .
chezmoi verify

-- home/user/.dir/file --
# contents of .dir/file
-- home/user/.local/share/chezmoi/symlink_dot_symlink_forward_slash.tmpl --
{{ env "HOME" }}/.dir/file
